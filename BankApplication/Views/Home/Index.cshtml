@model Bank

@{
    ViewData["Title"] = "Home Page";
}

    <div class="text-center">
        <h1 class="display-4">Welcome</h1>
        <p>@Model.Title</p>
        <p>Адрес: @Model.Address</p>
    </div>
